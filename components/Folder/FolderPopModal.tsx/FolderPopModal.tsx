import { ModalInterface } from "@/types/types";

export default function FolderPopModal({ onClick }: { onClick: (m: ModalInterface) => void}) {
  const handleCreate = (id: string) => {
    if (id === "folderCreate") {
      const newModal: ModalInterface = {
        component: <A
      }
    }
  }
}