{
  "compilerOptions": {
    "target": "ES5",
    "module": "CommonJS",
    "strict": true,
    "allowJs": true,
    "esModuleInterop": true,
    "jsx": "react-jsx"
  },
  "include": ["src", "netx-env.d.ts", "**/*.ts", "**/*.tsx"],
}
// 개별파일로 하니씩 ts로 바꾸기
//esmoduleinterop:  TypeScript는 CommonJS 모듈을 ES 모듈로 가져올 때 import 구문을 사용할 수 있게 된다
// 타입스크립트 컴파일러는 jsx문법을 해석할 수 없기 때문에 "--jsx 플래그를 제공하지 않으면 jsx를 사용할 수 없다" 에러가 발생
//    "jsx": "react-jsx" 는 리액트에서 jsx를 사용할 수 있게 해주는 옵션

//타입 선언 패키지 설치
//tsconfig.json 파일에 만들기
//모든 js파일을 jsx파일로 만들고
//개별하나씩 tsx파일로 만들기

//state의 값을 정하지 않으면 기본적으로 underfined가 된다
//useState<타입>()으로 지정해줄수있음

//이벤트 객체의 타입은 React.MouseEvent<HTMLButtonElement>처럼 지정해줄수있음